/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ContactTypeaheadDirective } from './contact-typeahead.directive';

describe('ContactTypeaheadDirective', () => {
  it('should create an instance', () => {
    let directive = new ContactTypeaheadDirective();
    expect(directive).toBeTruthy();
  });
});
